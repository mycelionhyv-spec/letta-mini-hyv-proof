# Five-department proof verification — 2026-09-23

Base: PR #2, `five-agents/edwin-agnes-clean` at `e271365e315f9a1286ad590f051f06b3d36b864d`.
The archived 2026-09-22 run is retained unmodified. This review is independent of its narrative verdict.

## Check results

| Check | Result | Evidence and limit |
|---|---|---|
| Blind private-memory isolation | **BLOCKED** | The old `04-isolation-*.json` prompts revealed their own answers, so their PASS is withdrawn. `src/run_blind_isolation.py` now generates fresh random synthetic markers, teaches Edwin, Agnes and Arthur separately, checks own recall after `--new`, and asks four blind cross questions: Agnes→Edwin, Edwin→Agnes, Arthur→Edwin and Edwin→Arthur. It records exact agent IDs, prompts, responses and raw calls in a fresh output directory. This verification workspace has no local Letta binary, original MemFS store or `XAI_API_KEY`, so no new model or namespace result exists. The runner explicitly labels storage separation UNVERIFIED. |
| Archived token reconciliation | **PASS** | `src/reconcile_usage.py` checks all 28 raw JSON records for successful return codes and complete usage fields. For each, `total_tokens = prompt_tokens + completion_tokens + cached_input_tokens`. Generated [per-call ledger](token-reconciliation.json) and `COST.md` agree. Ten offline tests pass, including a false-total rejection and a simulated full blind runner. Provider-billable tokens and monetary cost remain **unknown**, without an invoice. |

### Exact archived totals

| Raw usage field | Tokens |
|---|---:|
| `prompt_tokens` | 684,557 |
| `completion_tokens` | 6,555 |
| `cached_input_tokens` | 59,648 |
| `total_tokens` | 750,760 |
| `context_tokens` (separate, nonadditive) | 620,828 |
| `cache_write_tokens` / `reasoning_tokens` | 0 / 0 |

The 59,648 difference is the exact cached-input total. The `total_tokens` field does **not** mean the provider invoiced 750,760 tokens at a single uncached rate. The archive does not contain provider billing evidence.

## Finish the blocked check on Grok's original Letta machine

1. Verify that the five configured local agent IDs resolve to the existing agents and that a model credential is configured; do not copy it into the repository.
2. Run `python3 src/run_blind_isolation.py` once, creating a fresh evidence directory. The runner saves all prompts and raw replies. Do not reuse the old markers; the old prompts contaminated those sessions.
3. Inspect the **actual local MemFS namespaces read-only**. Record how agent IDs map to distinct storage paths/partitions, which agent can access which private marker, the commands or API used, and the result. Do not rely solely on a refusal from an LLM; do not infer cryptographic isolation from a directory name.
4. Treat own exact recall, blind cross denials without leaked markers, successful calls **and** the storage inspection as separate gates. If any part fails or is unavailable, retain BLOCKED/FAIL. Keep synthetic markers and credentials out of public website content.

## Remaining weaknesses

- The five-agent suite uses Python sequential routing. Native Letta Groups/A2A, parent-initiated delegation and autonomous collaboration have not been demonstrated.
- The archived Sabi decision check largely looks for department names; names alone cannot prove that each claim is supported. PR #1 adds receipt IDs and hashes, but its current prompt supplies the exact tags to copy. Trace claims to receipt content in further tests.
- Local agent IDs and memories are not portable by cloning GitHub. This workspace cannot rerun live checks. Five roles have not been connected to LifeOS, authenticated for an owner, deployed or exposed through a website.
- Per-call Letta token fields reconcile; provider pricing, cache discounts and actual expenditure require billing records. The observed system prompt cost remains material.

## Integration sequence

1. Finish the blind live run and read-only storage inspection. Keep its result separate from the archived run.
2. Combine PR #2's five-agent configuration with PR #1's custom receipt council on a new integration branch. PR #1 currently defaults to Arthur and Mr G with `max_calls=3`; four specialists plus Sabi need **at least five** model calls. Set measured model-call and token limits, explicit failure persistence and request-specific evidence.
3. Extend offline contract tests to route to all four distinct specialist IDs, preserve exact responses/hashes, require all four receipts, reject missing/forged receipts and failed/budget-exceeded calls, and test that Sabi does not attribute unsupported findings to any specialist. Run three bounded live requests as PR #1's promotion gate requires and inspect the raw evidence and costs.
4. Build an owner-authenticated Sabi API/service separately from the public design. Keep secrets and the Letta store server-side. Test signed-out rejection, owner-only access, data retention and failure handling. The public website can describe the department advantage without private bot names or claims of live integration.
5. Connect the site only to the verified owner route for a **private test** with clear test labeling and monitoring. Public customer access, payments and production claims require their own review and evidence.

**GO** for building an isolated, nonpublic integration test from a combined branch. **NO-GO** for claiming five production-ready website bots or launching public bot access now. Do not merge either PR as a live website integration or publish this proof as product functionality.
